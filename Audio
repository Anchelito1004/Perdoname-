<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width,initial-scale=1" />
  <title>Perdóname azul</title>
  <style>
    body {
      font-family: 'Georgia', serif;
      background: linear-gradient(120deg,#f7f3ea,#efe7dc);
      margin:0;
      display:flex;
      align-items:center;
      justify-content:center;
      min-height:100vh;
      padding:20px;
    }

    .carta {
      width:100%;
      max-width:720px;
      background:#fff8ec;
      border-radius:14px;
      padding:34px;
      box-shadow:0 18px 35px rgba(20,20,20,0.12);
      border:1px solid #f0e2cf;
      position:relative;
    }

    .carta::before{
      content:"";
      position:absolute;
      inset:10px;
      border-radius:10px;
      border:2px dashed rgba(160,130,100,0.18);
      pointer-events:none;
    }

    h1{
      margin:0 0 18px 0;
      font-family: "Brush Script MT", "Lucida Handwriting", cursive;
      font-size:40px;
      color:#8b5e3c;
      text-align:center;
    }

    p {
      color:#5a4333;
      line-height:1.7;
      white-space:pre-line;
      margin:0 0 22px 0;
      font-size:16px;
    }

    .controls {
      display:flex;
      gap:12px;
      align-items:center;
      justify-content:center;
      margin-top:18px;
    }

    a.play-link {
      text-decoration:none;
      background:#8b5e3c;
      color:#fff;
      padding:10px 18px;
      border-radius:10px;
      box-shadow:0 6px 12px rgba(0,0,0,0.12);
      font-weight:600;
    }

    button.play-btn {
      background:transparent;
      border:2px solid #8b5e3c;
      color:#8b5e3c;
      padding:9px 14px;
      border-radius:10px;
      cursor:pointer;
      font-weight:600;
    }

    audio#player {
      width:100%;
      margin-top:20px;
      display:block;
    }
  </style>
</head>
<body>
  <div class="carta" role="main">
    <h1>Perdóname azul</h1>

    <p>
Hola, perdon por irme, o sea la razon principal antes de cuando te dije de 2 semanas era por celos y por andar un poquitin triste y porque no queria ser una distraccion para tus examenes pero despues de que viera tantito r y ver como que me bloqueaste o algo asi si me senti mas triste y a eso pues sumale que tenia que estar mas ocupado porque ocupo dinero por algo de mi mami (e intento tener dinero) y he de admitir que me deje llevar por mis emociones ya que no quiero estar tanto tiempo sin decirte algo o ver lo que me decias asi que en verdad perdon por eso.

Me aprendi alguna cancion de tu lista de boywithuke asi que te mandare audios de las canciones que podia tocar al mi alcance para que me perdones te amo demasiado y de nuevo perdon por todo eso.
    </p>

    <div class="controls">
      <a href="#" class="play-link" id="playLink">▶ Reproducir audio</a>
    </div>

    <!-- Player oculto, se activa al hacer clic -->
    <audio id="player" preload="auto">
      <source src="9bbd480a-d10d-4a2d-9446-9b6a0b2ff017.mp3" type="audio/mpeg">
      Tu navegador no soporta audio.
    </audio>
  </div>

  <script>
    const playLink = document.getElementById('playLink');
    const player = document.getElementById('player');

    playLink.addEventListener('click', function(e){
      e.preventDefault();
      player.currentTime = 0;
      player.play().catch(err => {
        alert('El navegador bloqueó la reproducción automática. Intenta presionar reproducir manualmente.');
      });
    });
  </script>
</body>
</html>
